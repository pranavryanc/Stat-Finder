services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: stat_finder
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - stat_finder_pg:/var/lib/postgresql/data
volumes:
  stat_finder_pg:
